# Archived: meeting-scoped and point-in-time docs

Moved here 2026-08-04. Not deleted — kept for reference — but no longer
linked from the main README's doc table, since they describe a moment
in time rather than the project's current or ongoing state.

## Why archived

- **`PRE-MEETING-CHECKLIST.html`** — a prep list for the 2026-08-02 meeting
  with Nigel. That meeting happened; the checklist's job is done.
- **`QUESTIONS-FOR-NIGEL.html`** — specific questions drafted for that same
  meeting, grounded in what was implemented at the time. Answered; see
  the meeting outcome reflected in `docs/INTEGRATION.html` and
  `docs/ROADMAP.html`'s pivot notes instead.
- **`WHATS-NEW.html`** — a changelog snapshot as of the `Arya` branch point,
  already superseded by `docs/ROADMAP.html`'s "what's genuinely working
  today" section, which is kept current rather than frozen at one point.

## If you need them again

Nothing here was deleted — `git mv` preserved history, so `git log --follow`
on any file still shows its full history from before the move.
